# apiVersion: batch/v1
# kind: CronJob
# metadata:
#   name: cronjob-first
#   namespace: cron-jobs
# spec:
#   schedule: "*/5 * * * *"
#   timeZone: "Asia/Kolkata"
#   concurrencyPolicy: Forbid
#   successfulJobsHistoryLimit: 3
#   failedJobsHistoryLimit: 3
#   jobTemplate:
#     spec:
#       template:
#         spec:
#           containers:
#           - name: app
#             image: busybox
#             command:
#               - sh
#               - -c
#               - date; echo "Hello from GitOps CronJob"
#             resources:
#               requests:
#                 cpu: "100m"
#                 memory: "128Mi"
#           restartPolicy: OnFailure
# ---
# apiVersion: batch/v1
# kind: CronJob
# metadata:
#   name: cronjob-second
#   namespace: cron-jobs
# spec:
#   schedule: "*/2 * * * *"
#   timeZone: "Asia/Kolkata"
#   concurrencyPolicy: Forbid
#   successfulJobsHistoryLimit: 3
#   failedJobsHistoryLimit: 3

#   jobTemplate:
#     spec:
#       template:
#         spec:
#           containers:
#           - name: app
#             image: busybox
#             command:
#               - sh
#               - -c
#               - date; echo "Hello from GitOps CronJob"
#             resources:
#               requests:
#                 cpu: "100m"
#                 memory: "128Mi"
#           restartPolicy: OnFailure

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-third
  namespace: cron-jobs
spec:
  schedule: "*/1 * * * *"
  concurrencyPolicy: Forbid # Skip new job if previous still running
  # Options:
  # - Allow: Multiple Jobs can run simultaneously [ Default ]
  # - Forbid: Skip new Job if previous still running
  # - Replace: Cancel running Job, start new one
  successfulJobsHistoryLimit: 1  # Default: 3
  failedJobsHistoryLimit: 1
  activeDeadlineSeconds: 60 # Must start within 60 seconds
  backoffLimit: 2
  #suspend: true   # No new Jobs will be created
  timeZone: "America/New_York"    # Run in specific timezone
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: third
            image: busybox
            command:
              - /bin/sh
              - -c
              - date; echo "Hello from GitOps CronJob"
            resources:
              requests:
                cpu: "100m"
                memory: "128Mi"
          restartPolicy: OnFailure